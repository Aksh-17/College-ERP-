document.getElementById('loginForm').addEventListener('submit', (e)=>{ e.preventDefault(); const email = document.getElementById('email').value.trim(); const pwd = document.getElementById('password').value.trim(); if(!email||!pwd){ document.getElementById('error').textContent='Enter credentials'; return; } let role='student'; if(email.includes('admin')) role='admin'; if(email.includes('faculty')) role='faculty'; localStorage.setItem('userRole', role); localStorage.setItem('userEmail', email); if(email.includes('student1')) localStorage.setItem('userId','1'); if(email.includes('student2')) localStorage.setItem('userId','2'); window.location.href = role === 'admin' ? 'admin.html' : (role === 'faculty' ? 'faculty.html' : 'student.html'); }); function demoCreds(){ document.getElementById('email').value='student1@university.com'; document.getElementById('password').value='pass'; }